i have done the problems in node.js
for inputting data in the terminal i have used a package of node called readline-sync
readline sync can be installed in the command line using the command npm i readline-sync
